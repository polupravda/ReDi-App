import React from "react";
import Content from "./content";
import styled from "styled-components";

const StyledDiv = styled`
    font-size: 16px;
`;

function Card() {
  return <StyledDiv>Hello Styled Div</StyledDiv>;
}

export default Card;
